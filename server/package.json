{
  "name": "hazelnutpilot-ai-server",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "node index.js",
    "start": "node server/index.js",
    "build": "echo 'no build step for server'",
    "postinstall": "npx playwright install --with-deps || npx playwright install"
  },
  "dependencies": {
    "@playwright/test": "1.55.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mammoth": "^1.10.0",
    "morgan": "^1.10.1",
    "multer": "^1.4.5-lts.1",
    "nanoid": "^5.0.7",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
