services:
  - type: web
    name: hazelnutpilot-ai-api
    env: docker
    plan: free
    branch: main
    rootDir: server
    healthCheckPath: /api/summary
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENROUTER_API_KEY
        sync: false
